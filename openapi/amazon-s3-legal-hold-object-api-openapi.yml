openapi: 3.1.0
info:
  title: Amazon S3 Legal Hold (object-level)
  description: '2 operations from the Amazon S3 REST API, generated from the authoritative service model.


    S3 identifies these operations by the `legal-hold` query subresource, which is declared as a required
    query parameter on each one because an OpenAPI path cannot carry a query string.


    Generated from boto/botocore `service-2.json` at `aeb03fc4ae530e0b3f47d588b6021581db870c8c` (Apache-2.0).
    Responses are XML.'
  version: '2006-03-01'
  contact:
    name: AWS Support
    url: https://aws.amazon.com/premiumsupport/
externalDocs:
  description: Amazon S3 API Reference
  url: https://docs.aws.amazon.com/AmazonS3/latest/API/
servers:
- url: https://s3.{region}.amazonaws.com
  variables:
    region:
      default: us-east-1
tags:
- name: legal-hold
paths:
  /{Bucket}/{Key}:
    get:
      operationId: GetObjectLegalHold
      summary: This operation is not supported for directory buckets. Gets an object's current legal hold
        status. For more information,
      description: 'This operation is not supported for directory buckets. Gets an object''s current legal
        hold status. For more information, see Locking Objects. This functionality is not supported for
        Amazon S3 on Outposts. The following action is related to GetObjectLegalHold: GetObjectAttributes
        You must URL encode any signed header values that contain spaces. For example, if your header
        value is my file.txt, containing two spaces after my, you must URL encode this value to my%20%20file.txt.'
      tags:
      - legal-hold
      parameters:
      - name: legal-hold
        in: query
        required: true
        description: Selects this operation. S3 identifies it by the `legal-hold` subresource.
        schema:
          type: string
      - name: Bucket
        in: path
        required: true
        description: The bucket name containing the object whose legal hold status you want to retrieve.
          Access points - When you use this action with an access point for general purpose buckets, you
          must provide the alia
        schema:
          type: string
      - name: Key
        in: path
        required: true
        description: The key name for the object whose legal hold status you want to retrieve.
        schema:
          type: string
      - name: versionId
        in: query
        required: false
        description: The version ID of the object whose legal hold status you want to retrieve.
        schema:
          type: string
      - name: x-amz-request-payer
        in: header
        required: false
        schema:
          type: string
          enum: &id001
          - requester
      - name: x-amz-expected-bucket-owner
        in: header
        required: false
        description: The account ID of the expected bucket owner. If the account ID that you provide does
          not match the actual owner of the bucket, the request fails with the HTTP status code 403 Forbidden
          (access denied)
        schema:
          type: string
      responses:
        '200':
          description: Success. S3 answers in XML, not JSON.
          x-output-shape: GetObjectLegalHoldOutput
        '403':
          description: AccessDenied. S3 validates the signature before it routes the operation, so this
            does not distinguish an unsupported operation from an unauthorised one.
      x-s3-greedy-path-segment: 'AWS writes this route as `/{Bucket}/{Key+}`. The trailing `+` marks a
        greedy segment, which OpenAPI templating cannot express: an S3 object key may contain slashes,
        so this parameter is not a single path component.'
    put:
      operationId: PutObjectLegalHold
      summary: This operation is not supported for directory buckets. Applies a legal hold configuration
        to the specified object. For m
      description: This operation is not supported for directory buckets. Applies a legal hold configuration
        to the specified object. For more information, see Locking Objects. This functionality is not
        supported for Amazon S3 on Outposts. You must URL encode any signed header values that contain
        spaces. For example, if your header value is my file.txt, containing two spaces after my, you
        must URL encode this value to my%20%20file.txt.
      tags:
      - legal-hold
      parameters:
      - name: legal-hold
        in: query
        required: true
        description: Selects this operation. S3 identifies it by the `legal-hold` subresource.
        schema:
          type: string
      - name: Bucket
        in: path
        required: true
        description: The bucket name containing the object that you want to place a legal hold on. Access
          points - When you use this action with an access point for general purpose buckets, you must
          provide the alias of t
        schema:
          type: string
      - name: Key
        in: path
        required: true
        description: The key name for the object that you want to place a legal hold on.
        schema:
          type: string
      - name: x-amz-request-payer
        in: header
        required: false
        schema:
          type: string
          enum: *id001
      - name: versionId
        in: query
        required: false
        description: The version ID of the object that you want to place a legal hold on.
        schema:
          type: string
      - name: Content-MD5
        in: header
        required: false
        description: The MD5 hash for the request body. For requests made using the Amazon Web Services
          Command Line Interface (CLI) or Amazon Web Services SDKs, this field is calculated automatically.
        schema:
          type: string
      - name: x-amz-sdk-checksum-algorithm
        in: header
        required: false
        description: 'Indicates the algorithm used to create the checksum for the object when you use
          the SDK. This header will not provide any additional functionality if you don''t use the SDK.
          When you send this header, '
        schema:
          type: string
          enum:
          - CRC32
          - CRC32C
          - SHA1
          - SHA256
          - CRC64NVME
          - SHA512
          - MD5
          - XXHASH64
          - XXHASH3
          - XXHASH128
      - name: x-amz-expected-bucket-owner
        in: header
        required: false
        description: The account ID of the expected bucket owner. If the account ID that you provide does
          not match the actual owner of the bucket, the request fails with the HTTP status code 403 Forbidden
          (access denied)
        schema:
          type: string
      responses:
        '200':
          description: Success. S3 answers in XML, not JSON.
          x-output-shape: PutObjectLegalHoldOutput
        '403':
          description: AccessDenied. S3 validates the signature before it routes the operation, so this
            does not distinguish an unsupported operation from an unauthorised one.
      x-s3-greedy-path-segment: 'AWS writes this route as `/{Bucket}/{Key+}`. The trailing `+` marks a
        greedy segment, which OpenAPI templating cannot express: an S3 object key may contain slashes,
        so this parameter is not a single path component.'
