openapi: 3.1.0
info:
  title: Amazon S3 Metadatatable (bucket-level)
  description: '3 operations from the Amazon S3 REST API, generated from the authoritative service model.


    S3 identifies these operations by the `metadataTable` query subresource, which is declared as a required
    query parameter on each one because an OpenAPI path cannot carry a query string.


    Generated from boto/botocore `service-2.json` at `aeb03fc4ae530e0b3f47d588b6021581db870c8c` (Apache-2.0).
    Responses are XML.'
  version: '2006-03-01'
  contact:
    name: AWS Support
    url: https://aws.amazon.com/premiumsupport/
externalDocs:
  description: Amazon S3 API Reference
  url: https://docs.aws.amazon.com/AmazonS3/latest/API/
servers:
- url: https://s3.{region}.amazonaws.com
  variables:
    region:
      default: us-east-1
tags:
- name: metadataTable
paths:
  /{Bucket}:
    post:
      operationId: CreateBucketMetadataTableConfiguration
      summary: We recommend that you create your S3 Metadata configurations by using the V2 CreateBucketMetadataConfiguration
        API opera
      description: We recommend that you create your S3 Metadata configurations by using the V2 CreateBucketMetadataConfiguration
        API operation. We no longer recommend using the V1 CreateBucketMetadataTableConfiguration API
        operation. If you created your S3 Metadata configuration before July 15, 2025, we recommend that
        you delete and re-create your configuration by using CreateBucketMetadataConfiguration so that
        you can expire journal table records and create a live inventory table. Creates a V1 S3 Metadata
        configuration for a general purpose bucket. For more information, see Accelerating data discovery
        with S3 Metadata in the Amazon S3 User Guide. Permissions To use this operation, you must have
        the following permissions. For more information, see Setting up permissions for configuring metadata
        tables in the Amazon S3 User Guide. If you want to encrypt your metadata tables with server-side
        encryption with Key Management Service (KMS) keys (SSE-KMS), you need additional permissions.
        For more information, see Setting up permissions for configuring metadata tables in the Amazon
        S3 User Guide. If you also want to integrate your table bucket with Amazon Web Services analytics
        services so that you can que
      tags:
      - metadataTable
      parameters:
      - name: metadataTable
        in: query
        required: true
        description: Selects this operation. S3 identifies it by the `metadataTable` subresource.
        schema:
          type: string
      - name: Bucket
        in: path
        required: true
        description: The general purpose bucket that you want to create the metadata table configuration
          for.
        schema:
          type: string
      - name: Content-MD5
        in: header
        required: false
        description: The Content-MD5 header for the metadata table configuration.
        schema:
          type: string
      - name: x-amz-sdk-checksum-algorithm
        in: header
        required: false
        description: The checksum algorithm to use with your metadata table configuration.
        schema:
          type: string
          enum:
          - CRC32
          - CRC32C
          - SHA1
          - SHA256
          - CRC64NVME
          - SHA512
          - MD5
          - XXHASH64
          - XXHASH3
          - XXHASH128
      - name: x-amz-expected-bucket-owner
        in: header
        required: false
        description: The expected owner of the general purpose bucket that corresponds to your metadata
          table configuration.
        schema:
          type: string
      responses:
        '200':
          description: Success. S3 answers in XML, not JSON.
        '403':
          description: AccessDenied. S3 validates the signature before it routes the operation, so this
            does not distinguish an unsupported operation from an unauthorised one.
    delete:
      operationId: DeleteBucketMetadataTableConfiguration
      summary: 'We recommend that you delete your S3 Metadata configurations by using the V2 DeleteBucketMetadataTableConfiguration
        API '
      description: We recommend that you delete your S3 Metadata configurations by using the V2 DeleteBucketMetadataTableConfiguration
        API operation. We no longer recommend using the V1 DeleteBucketMetadataTableConfiguration API
        operation. If you created your S3 Metadata configuration before July 15, 2025, we recommend that
        you delete and re-create your configuration by using CreateBucketMetadataConfiguration so that
        you can expire journal table records and create a live inventory table. Deletes a V1 S3 Metadata
        configuration from a general purpose bucket. For more information, see Accelerating data discovery
        with S3 Metadata in the Amazon S3 User Guide. You can use the V2 DeleteBucketMetadataConfiguration
        API operation with V1 or V2 metadata table configurations. However, if you try to use the V1 DeleteBucketMetadataTableConfiguration
        API operation with V2 configurations, you will receive an HTTP 405 Method Not Allowed error. Make
        sure that you update your processes to use the new V2 API operations (CreateBucketMetadataConfiguration,
        GetBucketMetadataConfiguration, and DeleteBucketMetadataConfiguration) instead of the V1 API operations.
        Permissions To use this operation, you must have the s3:DeleteB
      tags:
      - metadataTable
      parameters:
      - name: metadataTable
        in: query
        required: true
        description: Selects this operation. S3 identifies it by the `metadataTable` subresource.
        schema:
          type: string
      - name: Bucket
        in: path
        required: true
        description: The general purpose bucket that you want to remove the metadata table configuration
          from.
        schema:
          type: string
      - name: x-amz-expected-bucket-owner
        in: header
        required: false
        description: The expected bucket owner of the general purpose bucket that you want to remove the
          metadata table configuration from.
        schema:
          type: string
      responses:
        '200':
          description: Success. S3 answers in XML, not JSON.
        '403':
          description: AccessDenied. S3 validates the signature before it routes the operation, so this
            does not distinguish an unsupported operation from an unauthorised one.
    get:
      operationId: GetBucketMetadataTableConfiguration
      summary: We recommend that you retrieve your S3 Metadata configurations by using the V2 GetBucketMetadataTableConfiguration
        API o
      description: We recommend that you retrieve your S3 Metadata configurations by using the V2 GetBucketMetadataTableConfiguration
        API operation. We no longer recommend using the V1 GetBucketMetadataTableConfiguration API operation.
        If you created your S3 Metadata configuration before July 15, 2025, we recommend that you delete
        and re-create your configuration by using CreateBucketMetadataConfiguration so that you can expire
        journal table records and create a live inventory table. Retrieves the V1 S3 Metadata configuration
        for a general purpose bucket. For more information, see Accelerating data discovery with S3 Metadata
        in the Amazon S3 User Guide. You can use the V2 GetBucketMetadataConfiguration API operation with
        V1 or V2 metadata table configurations. However, if you try to use the V1 GetBucketMetadataTableConfiguration
        API operation with V2 configurations, you will receive an HTTP 405 Method Not Allowed error. Make
        sure that you update your processes to use the new V2 API operations (CreateBucketMetadataConfiguration,
        GetBucketMetadataConfiguration, and DeleteBucketMetadataConfiguration) instead of the V1 API operations.
        Permissions To use this operation, you must have the s3:GetBucketMetad
      tags:
      - metadataTable
      parameters:
      - name: metadataTable
        in: query
        required: true
        description: Selects this operation. S3 identifies it by the `metadataTable` subresource.
        schema:
          type: string
      - name: Bucket
        in: path
        required: true
        description: The general purpose bucket that corresponds to the metadata table configuration that
          you want to retrieve.
        schema:
          type: string
      - name: x-amz-expected-bucket-owner
        in: header
        required: false
        description: The expected owner of the general purpose bucket that you want to retrieve the metadata
          table configuration for.
        schema:
          type: string
      responses:
        '200':
          description: Success. S3 answers in XML, not JSON.
          x-output-shape: GetBucketMetadataTableConfigurationOutput
        '403':
          description: AccessDenied. S3 validates the signature before it routes the operation, so this
            does not distinguish an unsupported operation from an unauthorised one.
