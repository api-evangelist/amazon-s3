openapi: 3.1.0
info:
  title: Amazon S3 Annotation (object-level)
  description: '3 operations from the Amazon S3 REST API, generated from the authoritative service model.


    S3 identifies these operations by the `annotation` query subresource, which is declared as a required
    query parameter on each one because an OpenAPI path cannot carry a query string.


    1 further operation(s) share a method, path and subresource with one described here and are recorded
    in `x-s3-sibling-operations`: S3 separates them by a request header or an id parameter, which OpenAPI
    cannot express.


    Generated from boto/botocore `service-2.json` at `aeb03fc4ae530e0b3f47d588b6021581db870c8c` (Apache-2.0).
    Responses are XML.'
  version: '2006-03-01'
  contact:
    name: AWS Support
    url: https://aws.amazon.com/premiumsupport/
externalDocs:
  description: Amazon S3 API Reference
  url: https://docs.aws.amazon.com/AmazonS3/latest/API/
servers:
- url: https://s3.{region}.amazonaws.com
  variables:
    region:
      default: us-east-1
tags:
- name: annotation
paths:
  /{Bucket}/{Key}:
    delete:
      operationId: DeleteObjectAnnotation
      summary: Deletes a specific annotation from an Amazon S3 object. Use the x-amz-object-if-match header
        to perform a conditional de
      description: 'Deletes a specific annotation from an Amazon S3 object. Use the x-amz-object-if-match
        header to perform a conditional delete that only succeeds if the object''s ETag matches the provided
        value, preventing race conditions during concurrent updates. Deleting an annotation is permanent.
        Annotations are not independently versioned, so there is no delete marker or way to recover a
        deleted annotation. To use this operation, you must have the s3:DeleteObjectAnnotation permission.
        If the object is protected by Object Lock in governance mode, you must also include the x-amz-bypass-governance-retention
        header. Annotations are not supported by the following features: S3 Inventory Reports, API Gateway,
        S3 Storage Lens, Amazon S3 File Gateway, Amazon FSx, S3 on Outposts, and S3 Express One Zone (directory
        buckets). The following operations are related to DeleteObjectAnnotation: PutObjectAnnotation
        GetObjectAnnotation ListObjectAnnotations'
      tags:
      - annotation
      parameters:
      - name: annotation
        in: query
        required: true
        description: Selects this operation. S3 identifies it by the `annotation` subresource.
        schema:
          type: string
      - name: Bucket
        in: path
        required: true
        description: The name of the bucket that contains the object.
        schema:
          type: string
      - name: Key
        in: path
        required: true
        description: The object key.
        schema:
          type: string
      - name: annotationName
        in: query
        required: true
        description: 'The name of the annotation to delete. Annotation names are UTF-8 encoded and cannot
          start with aws or s3 (case-insensitive). Length Constraints: Minimum length of 1. Maximum length
          of 512 bytes.'
        schema:
          type: string
      - name: versionId
        in: query
        required: false
        description: The version ID of the object.
        schema:
          type: string
      - name: x-amz-request-payer
        in: header
        required: false
        schema:
          type: string
          enum: &id001
          - requester
      - name: x-amz-expected-bucket-owner
        in: header
        required: false
        description: The account ID of the expected bucket owner.
        schema:
          type: string
      - name: x-amz-object-if-match
        in: header
        required: false
        description: If specified, the operation only succeeds if the object's ETag matches the provided
          value.
        schema:
          type: string
      responses:
        '200':
          description: Success. S3 answers in XML, not JSON.
          x-output-shape: DeleteObjectAnnotationOutput
        '403':
          description: AccessDenied. S3 validates the signature before it routes the operation, so this
            does not distinguish an unsupported operation from an unauthorised one.
      x-s3-greedy-path-segment: 'AWS writes this route as `/{Bucket}/{Key+}`. The trailing `+` marks a
        greedy segment, which OpenAPI templating cannot express: an S3 object key may contain slashes,
        so this parameter is not a single path component.'
    get:
      operationId: GetObjectAnnotation
      summary: Retrieves an annotation from an Amazon S3 object. To use this operation, you must have
        the s3:GetObjectAnnotation permis
      description: 'Retrieves an annotation from an Amazon S3 object. To use this operation, you must
        have the s3:GetObjectAnnotation permission. If checksum mode is enabled via the x-amz-checksum-mode
        header, Amazon S3 returns the stored checksum in the response headers for client-side validation.
        Annotations are not supported by the following features: S3 Inventory Reports, API Gateway, S3
        Storage Lens, Amazon S3 File Gateway, Amazon FSx, S3 on Outposts, and S3 Express One Zone (directory
        buckets). The following operations are related to GetObjectAnnotation: PutObjectAnnotation ListObjectAnnotations
        DeleteObjectAnnotation'
      tags:
      - annotation
      parameters:
      - name: annotation
        in: query
        required: true
        description: Selects this operation. S3 identifies it by the `annotation` subresource.
        schema:
          type: string
      - name: Bucket
        in: path
        required: true
        description: The name of the bucket that contains the object.
        schema:
          type: string
      - name: Key
        in: path
        required: true
        description: The object key.
        schema:
          type: string
      - name: annotationName
        in: query
        required: true
        description: 'The name of the annotation to retrieve. Length Constraints: Minimum length of 1.
          Maximum length of 512 bytes.'
        schema:
          type: string
      - name: versionId
        in: query
        required: false
        description: The version ID of the object.
        schema:
          type: string
      - name: x-amz-request-payer
        in: header
        required: false
        schema:
          type: string
          enum: *id001
      - name: x-amz-expected-bucket-owner
        in: header
        required: false
        description: The account ID of the expected bucket owner. If the bucket is owned by a different
          account, the request fails with an HTTP 403 (Access Denied) error.
        schema:
          type: string
      - name: x-amz-checksum-mode
        in: header
        required: false
        description: Set to ENABLED to validate the checksum of the annotation payload on retrieval.
        schema:
          type: string
          enum:
          - ENABLED
      responses:
        '200':
          description: Success. S3 answers in XML, not JSON.
          x-output-shape: GetObjectAnnotationOutput
        '403':
          description: AccessDenied. S3 validates the signature before it routes the operation, so this
            does not distinguish an unsupported operation from an unauthorised one.
      x-s3-greedy-path-segment: 'AWS writes this route as `/{Bucket}/{Key+}`. The trailing `+` marks a
        greedy segment, which OpenAPI templating cannot express: an S3 object key may contain slashes,
        so this parameter is not a single path component.'
    put:
      operationId: PutObjectAnnotation
      summary: Attaches an annotation to an Amazon S3 object. An annotation is a named payload of 1 byte
        to 1 MiB that you can associat
      description: 'Attaches an annotation to an Amazon S3 object. An annotation is a named payload of
        1 byte to 1 MiB that you can associate with a specific object or object version. Each object can
        have up to 1,000 annotations. For annotation naming rules and restrictions, see Annotation naming
        guidelines in the Amazon S3 User Guide. Annotations inherit the encryption of their parent object.
        For objects without server-side encryption, annotations are encrypted with SSE-S3 (the default
        for new objects). Objects encrypted with SSE-C cannot have annotations. To use this operation,
        you must have the s3:PutObjectAnnotation permission. If the bucket has Requester Pays enabled,
        you must include the x-amz-request-payer header. Annotations are not supported by the following
        features: S3 Inventory Reports, API Gateway, S3 Storage Lens, Amazon S3 File Gateway, Amazon FSx,
        S3 on Outposts, and S3 Express One Zone (directory buckets). The following operations are related
        to PutObjectAnnotation: GetObjectAnnotation ListObjectAnnotations DeleteObjectAnnotation'
      tags:
      - annotation
      parameters:
      - name: annotation
        in: query
        required: true
        description: Selects this operation. S3 identifies it by the `annotation` subresource.
        schema:
          type: string
      - name: Bucket
        in: path
        required: true
        description: The name of the bucket that contains the object.
        schema:
          type: string
      - name: Key
        in: path
        required: true
        description: The object key.
        schema:
          type: string
      - name: versionId
        in: query
        required: false
        description: The version ID of the object to attach the annotation to.
        schema:
          type: string
      - name: annotationName
        in: query
        required: true
        description: 'The name of the annotation. Length Constraints: Minimum length of 1. Maximum length
          of 512 bytes.'
        schema:
          type: string
      - name: x-amz-object-if-match
        in: header
        required: false
        description: If specified, the operation only succeeds if the object's ETag matches the provided
          value.
        schema:
          type: string
      - name: x-amz-sdk-checksum-algorithm
        in: header
        required: false
        description: 'The checksum algorithm to use. Supported values: CRC32, CRC32C, CRC64NVME, SHA1,
          SHA256, SHA512, MD5, XXHASH64, XXHASH3, XXHASH128.'
        schema:
          type: string
          enum:
          - CRC32
          - CRC32C
          - SHA1
          - SHA256
          - CRC64NVME
          - SHA512
          - MD5
          - XXHASH64
          - XXHASH3
          - XXHASH128
      - name: x-amz-checksum-crc32
        in: header
        required: false
        description: Base64-encoded CRC32 checksum of the annotation payload.
        schema:
          type: string
      - name: x-amz-checksum-crc32c
        in: header
        required: false
        description: Base64-encoded CRC32C checksum of the annotation payload.
        schema:
          type: string
      - name: x-amz-checksum-crc64nvme
        in: header
        required: false
        description: Base64-encoded CRC64NVME checksum of the annotation payload.
        schema:
          type: string
      - name: x-amz-checksum-sha1
        in: header
        required: false
        description: Base64-encoded SHA1 checksum of the annotation payload.
        schema:
          type: string
      - name: x-amz-checksum-sha256
        in: header
        required: false
        description: Base64-encoded SHA256 checksum of the annotation payload.
        schema:
          type: string
      - name: x-amz-checksum-sha512
        in: header
        required: false
        description: Base64-encoded SHA512 checksum of the annotation payload.
        schema:
          type: string
      - name: x-amz-checksum-md5
        in: header
        required: false
        description: Base64-encoded MD5 checksum of the annotation payload.
        schema:
          type: string
      - name: x-amz-checksum-xxhash64
        in: header
        required: false
        description: Base64-encoded XXHASH64 checksum of the annotation payload.
        schema:
          type: string
      - name: x-amz-checksum-xxhash3
        in: header
        required: false
        description: Base64-encoded XXHASH3 checksum of the annotation payload.
        schema:
          type: string
      - name: x-amz-checksum-xxhash128
        in: header
        required: false
        description: Base64-encoded XXHASH128 checksum of the annotation payload.
        schema:
          type: string
      - name: Content-MD5
        in: header
        required: false
        description: Base64-encoded MD5 digest of the message.
        schema:
          type: string
      - name: x-amz-request-payer
        in: header
        required: false
        schema:
          type: string
          enum: *id001
      - name: x-amz-expected-bucket-owner
        in: header
        required: false
        description: The account ID of the expected bucket owner. If the bucket is owned by a different
          account, the request fails with an HTTP 403 (Access Denied) error.
        schema:
          type: string
      responses:
        '200':
          description: Success. S3 answers in XML, not JSON.
          x-output-shape: PutObjectAnnotationOutput
        '403':
          description: AccessDenied. S3 validates the signature before it routes the operation, so this
            does not distinguish an unsupported operation from an unauthorised one.
      x-s3-greedy-path-segment: 'AWS writes this route as `/{Bucket}/{Key+}`. The trailing `+` marks a
        greedy segment, which OpenAPI templating cannot express: an S3 object key may contain slashes,
        so this parameter is not a single path component.'
x-s3-sibling-operations:
- operation: ListObjectAnnotations
  shadows: GetObjectAnnotation
  summary: Lists the annotations attached to an Amazon S3 object. Results are paginated, with a maximum
    of 1,000 annotations per object. Use the AnnotationPrefix parameter to filter the results by name
    prefix. T
  why: Same method, path and query subresource as the operation above. S3 separates them by a request
    header or by the presence of an id parameter, neither of which an OpenAPI path can express.
