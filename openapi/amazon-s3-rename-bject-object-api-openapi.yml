openapi: 3.1.0
info:
  title: Amazon S3 Renameobject (object-level)
  description: '1 operations from the Amazon S3 REST API, generated from the authoritative service model.


    S3 identifies these operations by the `renameObject` query subresource, which is declared as a required
    query parameter on each one because an OpenAPI path cannot carry a query string.


    Generated from boto/botocore `service-2.json` at `aeb03fc4ae530e0b3f47d588b6021581db870c8c` (Apache-2.0).
    Responses are XML.'
  version: '2006-03-01'
  contact:
    name: AWS Support
    url: https://aws.amazon.com/premiumsupport/
externalDocs:
  description: Amazon S3 API Reference
  url: https://docs.aws.amazon.com/AmazonS3/latest/API/
servers:
- url: https://s3.{region}.amazonaws.com
  variables:
    region:
      default: us-east-1
tags:
- name: renameObject
paths:
  /{Bucket}/{Key}:
    put:
      operationId: RenameObject
      summary: Renames an existing object in a directory bucket that uses the S3 Express One Zone storage
        class. You can use RenameObje
      description: Renames an existing object in a directory bucket that uses the S3 Express One Zone
        storage class. You can use RenameObject by specifying an existing object’s name as the source
        and the new name of the object as the destination within the same directory bucket. RenameObject
        is only supported for objects stored in the S3 Express One Zone storage class. To prevent overwriting
        an object, you can use the If-None-Match conditional header. If-None-Match - Renames the object
        only if an object with the specified name does not already exist in the directory bucket. If you
        don't want to overwrite an existing object, you can add the If-None-Match conditional header with
        the value ‘*’ in the RenameObject request. Amazon S3 then returns a 412 Precondition Failed error
        if the object with the specified name already exists. For more information, see RFC 7232. Permissions
        To grant access to the RenameObject operation on a directory bucket, we recommend that you use
        the CreateSession operation for session-based authorization. Specifically, you grant the s3express:CreateSession
        permission to the directory bucket in a bucket policy or an IAM identity-based policy. Then, you
        make the CreateSession API c
      tags:
      - renameObject
      parameters:
      - name: renameObject
        in: query
        required: true
        description: Selects this operation. S3 identifies it by the `renameObject` subresource.
        schema:
          type: string
      - name: Bucket
        in: path
        required: true
        description: The bucket name of the directory bucket containing the object. You must use virtual-hosted-style
          requests in the format Bucket-name.s3express-zone-id.region-code.amazonaws.com. Path-style requests
          are
        schema:
          type: string
      - name: Key
        in: path
        required: true
        description: Key name of the object to rename.
        schema:
          type: string
      - name: x-amz-rename-source
        in: header
        required: true
        description: Specifies the source for the rename operation. The value must be URL encoded.
        schema:
          type: string
      - name: If-Match
        in: header
        required: false
        description: Renames the object only if the ETag (entity tag) value provided during the operation
          matches the ETag of the object in S3. The If-Match header field makes the request method conditional
          on ETags. If t
        schema:
          type: string
      - name: If-None-Match
        in: header
        required: false
        description: Renames the object only if the destination does not already exist in the specified
          directory bucket. If the object does exist when you send a request with If-None-Match:*, the
          S3 API will return a 412
        schema:
          type: string
      - name: If-Modified-Since
        in: header
        required: false
        description: Renames the object if the destination exists and if it has been modified since the
          specified time.
        schema:
          type: string
      - name: If-Unmodified-Since
        in: header
        required: false
        description: Renames the object if it hasn't been modified since the specified time.
        schema:
          type: string
      - name: x-amz-rename-source-if-match
        in: header
        required: false
        description: Renames the object if the source exists and if its entity tag (ETag) matches the
          specified ETag.
        schema:
          type: string
      - name: x-amz-rename-source-if-none-match
        in: header
        required: false
        description: Renames the object if the source exists and if its entity tag (ETag) is different
          than the specified ETag. If an asterisk (*) character is provided, the operation will fail and
          return a 412 Preconditi
        schema:
          type: string
      - name: x-amz-rename-source-if-modified-since
        in: header
        required: false
        description: Renames the object if the source exists and if it has been modified since the specified
          time.
        schema:
          type: string
      - name: x-amz-rename-source-if-unmodified-since
        in: header
        required: false
        description: Renames the object if the source exists and hasn't been modified since the specified
          time.
        schema:
          type: string
      - name: x-amz-client-token
        in: header
        required: false
        description: 'A unique string with a max of 64 ASCII characters in the ASCII range of 33 - 126.
          RenameObject supports idempotency using a client token. To make an idempotent API request using
          RenameObject, specify '
        schema:
          type: string
      responses:
        '200':
          description: Success. S3 answers in XML, not JSON.
          x-output-shape: RenameObjectOutput
        '403':
          description: AccessDenied. S3 validates the signature before it routes the operation, so this
            does not distinguish an unsupported operation from an unauthorised one.
      x-s3-greedy-path-segment: 'AWS writes this route as `/{Bucket}/{Key+}`. The trailing `+` marks a
        greedy segment, which OpenAPI templating cannot express: an S3 object key may contain slashes,
        so this parameter is not a single path component.'
