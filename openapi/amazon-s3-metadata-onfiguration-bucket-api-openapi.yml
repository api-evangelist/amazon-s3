openapi: 3.1.0
info:
  title: Amazon S3 Metadataconfiguration (bucket-level)
  description: '3 operations from the Amazon S3 REST API, generated from the authoritative service model.


    S3 identifies these operations by the `metadataConfiguration` query subresource, which is declared
    as a required query parameter on each one because an OpenAPI path cannot carry a query string.


    Generated from boto/botocore `service-2.json` at `aeb03fc4ae530e0b3f47d588b6021581db870c8c` (Apache-2.0).
    Responses are XML.'
  version: '2006-03-01'
  contact:
    name: AWS Support
    url: https://aws.amazon.com/premiumsupport/
externalDocs:
  description: Amazon S3 API Reference
  url: https://docs.aws.amazon.com/AmazonS3/latest/API/
servers:
- url: https://s3.{region}.amazonaws.com
  variables:
    region:
      default: us-east-1
tags:
- name: metadataConfiguration
paths:
  /{Bucket}:
    post:
      operationId: CreateBucketMetadataConfiguration
      summary: Creates an S3 Metadata V2 metadata configuration for a general purpose bucket. For more
        information, see Accelerating da
      description: Creates an S3 Metadata V2 metadata configuration for a general purpose bucket. For
        more information, see Accelerating data discovery with S3 Metadata in the Amazon S3 User Guide.
        Permissions To use this operation, you must have the following permissions. For more information,
        see Setting up permissions for configuring metadata tables in the Amazon S3 User Guide. If you
        want to encrypt your metadata tables with server-side encryption with Key Management Service (KMS)
        keys (SSE-KMS), you need additional permissions in your KMS key policy. For more information,
        see Setting up permissions for configuring metadata tables in the Amazon S3 User Guide. If you
        also want to integrate your table bucket with Amazon Web Services analytics services so that you
        can query your metadata table, you need additional permissions. For more information, see Integrating
        Amazon S3 Tables with Amazon Web Services analytics services in the Amazon S3 User Guide. To query
        your metadata tables, you need additional permissions. For more information, see Permissions for
        querying metadata tables in the Amazon S3 User Guide. s3:CreateBucketMetadataTableConfiguration
        The IAM policy action name is the same for the V1
      tags:
      - metadataConfiguration
      parameters:
      - name: metadataConfiguration
        in: query
        required: true
        description: Selects this operation. S3 identifies it by the `metadataConfiguration` subresource.
        schema:
          type: string
      - name: Bucket
        in: path
        required: true
        description: The general purpose bucket that you want to create the metadata configuration for.
        schema:
          type: string
      - name: Content-MD5
        in: header
        required: false
        description: The Content-MD5 header for the metadata configuration.
        schema:
          type: string
      - name: x-amz-sdk-checksum-algorithm
        in: header
        required: false
        description: The checksum algorithm to use with your metadata configuration.
        schema:
          type: string
          enum:
          - CRC32
          - CRC32C
          - SHA1
          - SHA256
          - CRC64NVME
          - SHA512
          - MD5
          - XXHASH64
          - XXHASH3
          - XXHASH128
      - name: x-amz-expected-bucket-owner
        in: header
        required: false
        description: The expected owner of the general purpose bucket that corresponds to your metadata
          configuration.
        schema:
          type: string
      responses:
        '200':
          description: Success. S3 answers in XML, not JSON.
        '403':
          description: AccessDenied. S3 validates the signature before it routes the operation, so this
            does not distinguish an unsupported operation from an unauthorised one.
    delete:
      operationId: DeleteBucketMetadataConfiguration
      summary: Deletes an S3 Metadata configuration from a general purpose bucket. For more information,
        see Accelerating data discover
      description: 'Deletes an S3 Metadata configuration from a general purpose bucket. For more information,
        see Accelerating data discovery with S3 Metadata in the Amazon S3 User Guide. You can use the
        V2 DeleteBucketMetadataConfiguration API operation with V1 or V2 metadata configurations. However,
        if you try to use the V1 DeleteBucketMetadataTableConfiguration API operation with V2 configurations,
        you will receive an HTTP 405 Method Not Allowed error. Permissions To use this operation, you
        must have the s3:DeleteBucketMetadataTableConfiguration permission. For more information, see
        Setting up permissions for configuring metadata tables in the Amazon S3 User Guide. The IAM policy
        action name is the same for the V1 and V2 API operations. The following operations are related
        to DeleteBucketMetadataConfiguration: CreateBucketMetadataConfiguration GetBucketMetadataConfiguration
        UpdateBucketMetadataInventoryTableConfiguration UpdateBucketMetadataJournalTableConfiguration
        You must URL encode any signed header values that contain spaces. For example, if your header
        value is my file.txt, containing two spaces after my, you must URL encode this value to my%20%20file.txt.'
      tags:
      - metadataConfiguration
      parameters:
      - name: metadataConfiguration
        in: query
        required: true
        description: Selects this operation. S3 identifies it by the `metadataConfiguration` subresource.
        schema:
          type: string
      - name: Bucket
        in: path
        required: true
        description: The general purpose bucket that you want to remove the metadata configuration from.
        schema:
          type: string
      - name: x-amz-expected-bucket-owner
        in: header
        required: false
        description: The expected bucket owner of the general purpose bucket that you want to remove the
          metadata table configuration from.
        schema:
          type: string
      responses:
        '200':
          description: Success. S3 answers in XML, not JSON.
        '403':
          description: AccessDenied. S3 validates the signature before it routes the operation, so this
            does not distinguish an unsupported operation from an unauthorised one.
    get:
      operationId: GetBucketMetadataConfiguration
      summary: Retrieves the S3 Metadata configuration for a general purpose bucket. For more information,
        see Accelerating data discov
      description: 'Retrieves the S3 Metadata configuration for a general purpose bucket. For more information,
        see Accelerating data discovery with S3 Metadata in the Amazon S3 User Guide. You can use the
        V2 GetBucketMetadataConfiguration API operation with V1 or V2 metadata configurations. However,
        if you try to use the V1 GetBucketMetadataTableConfiguration API operation with V2 configurations,
        you will receive an HTTP 405 Method Not Allowed error. Permissions To use this operation, you
        must have the s3:GetBucketMetadataTableConfiguration permission. For more information, see Setting
        up permissions for configuring metadata tables in the Amazon S3 User Guide. The IAM policy action
        name is the same for the V1 and V2 API operations. The following operations are related to GetBucketMetadataConfiguration:
        CreateBucketMetadataConfiguration DeleteBucketMetadataConfiguration UpdateBucketMetadataInventoryTableConfiguration
        UpdateBucketMetadataJournalTableConfiguration You must URL encode any signed header values that
        contain spaces. For example, if your header value is my file.txt, containing two spaces after
        my, you must URL encode this value to my%20%20file.txt.'
      tags:
      - metadataConfiguration
      parameters:
      - name: metadataConfiguration
        in: query
        required: true
        description: Selects this operation. S3 identifies it by the `metadataConfiguration` subresource.
        schema:
          type: string
      - name: Bucket
        in: path
        required: true
        description: The general purpose bucket that corresponds to the metadata configuration that you
          want to retrieve.
        schema:
          type: string
      - name: x-amz-expected-bucket-owner
        in: header
        required: false
        description: The expected owner of the general purpose bucket that you want to retrieve the metadata
          table configuration for.
        schema:
          type: string
      responses:
        '200':
          description: Success. S3 answers in XML, not JSON.
          x-output-shape: GetBucketMetadataConfigurationOutput
        '403':
          description: AccessDenied. S3 validates the signature before it routes the operation, so this
            does not distinguish an unsupported operation from an unauthorised one.
